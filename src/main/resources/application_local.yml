spring:
  application:
    name: reservation-api
  profiles:
    active: local
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.H2Dialect
  datasource:
    url: jdbc:h2:C:/Users/zhack/AntajiaSoftware/reservationapi/src/main/resources/database/reservations
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true